import { total } from 'ember-books/helpers/total';
import { module, test } from 'qunit';

module('Unit | Helper | total');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = total([42]);
  assert.ok(result);
});
